# ManimTesting

This repository provides a simple Manim scene that demonstrates a few basic animations.

## Usage

You need Manim installed to render the animation. Run the scene with:

```bash
manim manim.py BeautifulAnimation -p -ql
```

This will open a preview window after rendering at low quality. Adjust the quality settings as desired.
